<nav class="w-full md:w-64 min-h-screen bg-proyecto-primary text-proyecto-bg p-4 space-y-4">
  <h2 class="text-xl font-semibold text-proyecto-secondary"><%= t(".title") %></h2>

  <%= link_to backstore_root_path,
              class: "block px-4 py-2 rounded flex items-center gap-3 transition-colors #{active_link?(backstore_root_path)}" do %>
    <i class="fa-solid fa-gauge"></i>
    <span><%= t(".dashboard") %></span>
  <% end %>

  <% if can? :read, Product %>
    <%= link_to backstore_products_path,
                class: "block px-4 py-2 rounded flex items-center gap-3 transition-colors #{active_link?(backstore_products_path)}" do %>
      <i class="fa-solid fa-compact-disc"></i>
      <span><%= t(".products") %></span>
    <% end %>
  <% end %>

  <% if can? :read, Sale %>
    <%= link_to backstore_sales_path,
                class: "block px-4 py-2 rounded flex items-center gap-3 transition-colors #{active_link?(backstore_sales_path)}" do %>
      <i class="fa-solid fa-cart-shopping"></i>
      <span><%= t(".sales") %></span>
    <% end %>
  <% end %>

  <% if can? :read, :reports %>
    <%= link_to backstore_reports_path,
                class: "block px-4 py-2 rounded flex items-center gap-3 transition-colors #{active_link?(backstore_reports_path)}" do %>
      <i class="fa-solid fa-chart-column"></i>
      <span><%= t(".reports") %></span>
    <% end %>
  <% end %>

  <% if can? :read, User %>
    <%= link_to backstore_users_path,
                class: "block px-4 py-2 rounded flex items-center gap-3 transition-colors #{active_link?(backstore_users_path)}" do %>
      <i class="fa-solid fa-users"></i>
      <span><%= t(".users") %></span>
    <% end %>
  <% end %>

  <hr class="border-proyecto-secondary/40 my-4">
</nav>
